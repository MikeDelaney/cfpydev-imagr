language: python
python:
  - "2.7"
# command to install dependencies
install: "pip install -r requirements.txt"
# command to run tests
script: python python cfpydev-imagr/ test imagr_images
branches:
    only:
      - models